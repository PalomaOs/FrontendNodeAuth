<!DOCTYPE html>
<html lang="es" data-bs-theme="dark">

<head>
    <meta charset="utf-8" />
    <link rel="icon" href="<%=locals.AppPath %>/favicon.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Peliculas Fei">
    <title>Netflix: <%= typeof locals.Title !='undefined' ? locals.Title: '' %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.3/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <%if (locals.IsAuthenticated == true) {%>
            <link rel="stylesheet" href="<%=locals.AppPath%>/css/site.css" />
    <%} else {%>
        <link rel="stylesheet" href="<%=locals.AppPath%>/css/guest.css" />
    <%}%>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-md bg-transparent">
            <div class="container-fluid">
                <a class="navbar-brand" href="<%=locals.AppPath%>/">
                    <img alt="Inicio" src="<%=locals.AppPath%>/images/logo.png" height="25">
                </a>
                <button class="navbar-toggler shadow-none border-0 btn btn-ligh" type="button" data-bs-toggle="collapse"
                    data-bs-target=".navbar-collapse" aria-controls="navbar SupportedContent" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <i class="bi bi-three-dots"></i>
                </button>
                <div class="navbar-collapse collapse">
                    <%- include("menuizq") %>
                    <%- include("menuder") %>
                </div>
            </div>
        </nav>
    </header>
    <main role="main" class="mt-3 mb-5 container">